springdoc:
  packagesToScan: com.example.livraria
spring:
  h2:
    console:
      enabled: true
      path: /h2
  datasource:
    url: jdbc:h2:file:./data/db-livraria
    driverClassName: org.h2.Driver
    username: th
    password: password
  jpa:
    database-platform: org.hibernate.dialect.H2Dialect
    hibernate:
      ddl-auto: update
